/** @type {import('next').NextConfig} */
const nextConfig = {
    output: "export",
    assetPrefix: "/build",
    distDir: "_dist",
 
    compress: true,
    output: "export",
 
}

module.exports = nextConfig

